# Pipeline Configuration

common:
  dataset: "game"
  gpu_id: 0
  seed: 0

bigrec:
  base_model: "Qwen/Qwen2-0.5B"
  sample: 1024
  batch_size: 128
  micro_batch_size: 4
  num_epochs: 50

inference:
  # Batch size for inference (generating distillation data)
  batch_size: 32

dllm2rec:
  model_name: "SASRec"
